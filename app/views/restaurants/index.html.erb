<h1>Mes restaurants</h1>
<% @restaurants.each do |restaurant| %>
  <h2><%= restaurant.name %></h2>
  <p><%= restaurant.address %></p>
  <p><%= "*" * restaurant.stars %></p>
  <%= link_to "Accéder à la page du restaurant", restaurant_path(restaurant) %>
<% end %>
